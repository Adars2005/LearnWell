# OpenRouter powers non-routine tutoring turns. Never commit a real key.
# Default model is the free router; set a specific paid model only when desired.
OPENROUTER_API_KEY=YOUR_OPENROUTER_API_KEY
OPENROUTER_MODEL=openrouter/free
